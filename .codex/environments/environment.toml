# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "tbt-linux"

[setup]
script = ""

[setup.win32]
script = '''
set -euo pipefail

export CODEX_CONDA_ENV=tbt

if [ -f "$HOME/miniconda3/etc/profile.d/conda.sh" ]; then
  source "$HOME/miniconda3/etc/profile.d/conda.sh"
elif [ -f "$HOME/anaconda3/etc/profile.d/conda.sh" ]; then
  source "$HOME/anaconda3/etc/profile.d/conda.sh"
elif [ -f "/opt/conda/etc/profile.d/conda.sh" ]; then
  source "/opt/conda/etc/profile.d/conda.sh"
else
  echo "[setup] conda.sh not found"
  exit 1
fi

conda activate "$CODEX_CONDA_ENV"

echo "[setup] active env: ${CONDA_DEFAULT_ENV:-unknown}"
python --version
which python
conda info --envs

python -c "import taichi" || pip install taichi
python -c "import numpy" || pip install numpy
python -c "import psutil" || pip install psutil
python -c "import pytest" || pip install pytest
python -c "import pandas" || pip install pandas
python -c "import matplotlib" || pip install matplotlib
python -c "import playwright" || pip install playwright
python -c "import scalene" || pip install scalene

playwright install chromium || true
'''
